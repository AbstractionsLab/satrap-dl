---
active: true
derived: false
level: 1.9
links:
- ARC-013: VrJXe20D8ZAhwSKLhvSJECUInchNpOJwqwPTc7jWtF0=
normative: true
ref: ''
reviewed: 0_AvLeZ6mvSWNmWRX7iq37UG0p7NINQsybFJ9GNN2_g=
---

# CTI analysis class diagram

The class diagram below depicts all classes involved when executing a Cyber Threat Intelligence analysis from the `CTIanalysisToolbox`; these classes are distributed across three architectural layers:

- **Service layer**: `CTIanalysisToolbox` - the public API for CTI analysis tasks
- **Engine layer**: `CTIEngine` - core reasoning and query execution
- **Data management layer**: `TypeDBQueryHandler` - database interaction

In addition, `InferredAnswer`, `Group`, `Mnemonic`, and `InferenceExplanation` are data classes for returned analysis results.

```mermaid
classDiagram
    class CTIanalysisToolbox {
        -cti_engine: CTIEngine
        +__init__(server: str, database: str)
        +explain_if_related_mitigation(group_mitre_id: str, mitigation_stix_id: str) InferredAnswer
        +explain_related_techniques(group_name: str, technique_id: str) InferredAnswer
        +explain_techniques_used_by_groups(groups: list, technique_id: str) InferredAnswer
        +get_mitre_id(stix_id: str) str
        +get_sdo_stats() str
        +get_techniques_used_by_all(groups: list, infer: bool) dict
        +mitigations_for_technique(stix_id: str) dict
        +mitre_attack_groups(keywords: list, all: bool) list~Group~
        +mitre_attack_mitigations() str
        +mitre_attack_techniques(subtechniques: bool) str
        +related_mitigations(group_id: str, group_name: str) dict
        +search_by_alias_name(keyword: str) list~Mnemonic~
        +search_by_mitre_id(mitre_id: str, ignore_revoked: bool) list~dict~
        +search_by_stix_id(stix_id: str) dict
        +summarize_techniques_usage(...) list
        +techniques_used_by_groups(group_ids: list, infer: bool) list
    }

    class CTIEngine {
        -server_address: str
        -database_name: str
        -db_manager: TypeDBQueryHandler
        +__init__(db_uri: str, db_name: str)
        +__enter__() CTIEngine
        +__exit__(exception_type, exception_value, traceback)
        +search_obj_by_stix_id(stix_id: str) dict
        +search_obj_by_attck_id(mitre_attck_id: str, ignore_revoked: bool) list~dict~
        +search_obj_by_name_alias(name_alias: str) list~Mnemonic~
        +get_creator(stix_id: str) str
        +get_external_references_src(stix_id: str) dict
        +get_mitre_id(stix_id: str) str
        +get_stats() dict
        +get_inference_rule_names() set
        +get_techniques_used_by(group_ids: list, inference: bool) dict
        +get_techniques_at_intersection(group_ids: list, inference: bool) dict
        +explain_techniques_used_by(groups: list, technique_id: str) InferredAnswer
        +get_intrusion_sets_per_technique(inference: bool, ignore_revoked: bool) dict
        +get_names_of_mitre_ids(attck_ids: list) dict
        +get_mitigations_for_sdo(stix_id: str) dict
        +get_mitig_rel_tech(group_mitre_id: str, group_name: str) dict
        +explain_mitig_rel_tech(group_stix_id: str, tech_stix_id: str) InferredAnswer
        +explain_group_rel_mitig(group_stix_id: str, mitigation_stix_id: str) InferredAnswer
        +get_all_mitigations() dict
        +get_all_techniques(subtechniques: bool) dict
        +filter_groups_keywords(keywords: list, all: bool) list~Group~
    }

    class TypeDBQueryHandler {
        -server_address: str
        -database_name: str
        -driver: TypeDB
        -session: Session
        +__init__(server_address: str, database_name: str)
        +get_attributes_of(entity: Entity) dict
        +get_query(query: str) list~ConceptMap~
        +fetch_query(query: str) list~ConceptMap~
        +aggregate_group_query(query: str) dict
        +get_attribute_value(query: str, attribute: str) list
        +get_inference_rules() set
        +close()
    }

    class InferredAnswer {
        query: str
        explanations: list~InferenceExplanation~
    }

    class InferenceExplanation {
        statement: str
        rule: str
        condition: str
        conclusion: str
        var_mapping: dict
        +as_json() dict
        +__str__() str
    }

    class Group {
        group_id: str
        name: str
        aliases: str
        description: str
        +as_tuple() tuple
        +__str__() str
    }

    class Mnemonic {
        stix_id: str
        name: str
        aliases: str
        +__str__() str
    }

    CTIanalysisToolbox --> CTIEngine : uses
    CTIEngine --> TypeDBQueryHandler : uses
    CTIEngine --> InferredAnswer : returns
    CTIEngine --> Group : returns
    CTIEngine --> Mnemonic : returns
    InferredAnswer --> InferenceExplanation : contains
    TypeDBQueryHandler --> Entity : manages
    TypeDBQueryHandler --> ConceptMap : returns
```
{width="70%"}

The CTI analysis process begins with the `CTIanalysisToolbox`, which exposes high-level analysis methods (e.g., `explain_techniques_used_by_groups`, `mitre_attack_groups`, `search_by_mitre_id`). Each method delegates to corresponding methods in the `CTIEngine`, which constructs TypeQL queries and executes them against the TypeDB knowledge base via the `TypeDBQueryHandler`.

Query results are retrieved as `ConceptMap` objects from TypeDB, then transformed into domain-specific result types:

- `InferredAnswer` - encapsulates query explanations when inference rules are applied
- `Group` - represents MITRE ATT&CK threat actor groups
- `Mnemonic` - represents STIX objects found by name or alias search
- Standard `dict` or `list` - for aggregated statistics and technique mappings

The `InferenceExplanation` dataclass provides detailed provenance for inferred facts, including the applied rule, satisfied conditions, and variable mappings between query and rule contexts.