---
active: true
derived: false
level: 2.2
links:
- ARC-008: tNGY6xykWhhyOON8NhypB6JdUFZeN9xtc1lv9xTwR5A=
normative: true
ref: ''
reviewed: G25q-NhlTfFPC6vSKMdVeKaUhAqW-MzWA4YhMkrysWg=
---

# Analysis layer class diagram

This diagram expands the **Analysis Layer** node from [SWD-011](SWD-011.md) into a UML class diagram, showing the concrete classes, their relationships, and the key attributes and operations exposed by each.

`AnalyzerRegistry` is the single static dispatcher: it holds class and instance registries keyed by `alert_type` and routes every analysis request. `BaseAnalyzer` defines the mandatory contract every analyzer must satisfy — a class-level `alert_type` string, a class-level `schema` (a Pydantic `BaseModel` subclass), and an abstract `analyze()` operation. `SuspiciousLoginAnalyzer` is a concrete implementation; it inherits `MISPEnrichmentMixin` to acquire lazy-initialised access to `MISPDataExtractor`. `SuspiciousLoginAlert` is the Pydantic input schema bound to that analyzer.

`AnalysisResult` (from `decipher.models`) is the uniform return type of every `analyze()` call and the response schema of the `/analyze` endpoint.

```mermaid
classDiagram
    class AnalyzerRegistry {
        <<service>>
        -_analyzers : dict~str, Type[BaseAnalyzer]~
        -_instances : dict~str, BaseAnalyzer~
        +register(analyzer_cls) Type[BaseAnalyzer]
        +get(alert_type) BaseAnalyzer | None
        +analyze(alert_type, data) AnalysisResult
        +list_types() list~str~
        +get_registered_classes() dict~str, Type[BaseAnalyzer]~
        +clear() None
    }

    class BaseAnalyzer {
        <<abstract>>
        +alert_type : str
        +schema : Type[BaseModel]
        +analyze(alert)* AnalysisResult
        +validate(data) BaseModel
    }

    class MISPEnrichmentMixin {
        -_misp_client : MISPDataExtractor | None
        +misp_client() MISPDataExtractor
        +search_iocs_in_misp(ioc_mapping, extra_search_params) list~EventData~
    }

    class SuspiciousLoginAnalyzer {
        +alert_type = "suspicious_login"
        +schema = SuspiciousLoginAlert
        +analyze(alert) AnalysisResult
        -_build_case_description(alert_data, score_res, analysis) str
    }

    class SuspiciousLoginAlert {
        <<schema>>
        +username : str
        +target_host : str
        +src_ips : list~str~
        +timestamp : str
    }

    class AnalysisResult {
        <<dataclass>>
        +analyzed_scenario : str
        +severity : float
        +report : dict
        +created_case : dict
        +__str__() str
    }

    class MISPDataExtractor {
        <<external>>
        +is_available() bool
        +bulk_search_iocs(iocs, extra_search_params) list~EventData~
    }

    class MISPEnrichmentError {
        <<exception>>
    }

    class UnavailableMISPClientError {
        <<exception>>
    }

    AnalyzerRegistry "1" o-- "0..*" BaseAnalyzer : registers / instantiates
    BaseAnalyzer <|-- SuspiciousLoginAnalyzer : extends
    MISPEnrichmentMixin <|-- SuspiciousLoginAnalyzer : mixes in
    SuspiciousLoginAnalyzer ..> SuspiciousLoginAlert : validates input via
    SuspiciousLoginAnalyzer ..> AnalysisResult : returns
    MISPEnrichmentMixin o-- MISPDataExtractor : lazy-init
    MISPEnrichmentError <|-- UnavailableMISPClientError : extends
    MISPEnrichmentMixin ..> MISPEnrichmentError : raises
```