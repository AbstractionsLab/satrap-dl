---
active: true
derived: false
level: 1.1
links:
- ARC-002: TQMEFSM4hhizeXvzXHXqlA-UCdT9CovaXqGZZtKGU6w=
- ARC-015: KwpFzaHS-6dWCYiG6xlNvcEST_K3Uxx_61dTol7V9yg=
normative: true
ref: ''
reviewed: Xho3bAP9GamWXVxkP0l2-JbBvW_T4h_MKeZOBi9XU4s=
---

# SATRAP package diagram

SATRAP is organized into the six main packages depicted below:

```mermaid
graph TB
    subgraph frontend["frontend"]
        cli["satrap_cli"]
        notebook["quick_start.ipynb"]
        commands["commands"]
    end

    subgraph service["service"]
        analysis["satrap_analysis<br/>(CTIanalysisToolbox)"]
    end

    subgraph engine["engine"]
        cti_engine["cti_engine<br/>(CTIEngine)"]
        result_struct["result_structures"]
        query_stmt["query_statements"]
    end

    subgraph commons["commons"]
        log_utils["log_utils"]
        exceptions["exceptions"]
        format_utils["format_utils"]
        file_utils["file_utils"]
        log_utils ~~~ exceptions ~~~ format_utils ~~~ file_utils
    end

    subgraph datamanagement["datamanagement/typedb"]
        handler["queryhandler"]
        manager["typedbmanager"]
        insert["inserthandler"]
        builder["typeql_builder"]
        constants["typedb_constants"]
        dataobjects["dataobjects"]
    end

    subgraph etl["etl"]
        subgraph extract["extract"]
            extractor["extractor"]
            extract_const["extract_constants"]
        end
        subgraph transform["transform"]
            orchestrator["etlorchestrator"]
            transformer["transformer"]
            mapper["stix_to_typedb_mapper"]
            converter["stixobject_converter"]
        end
        subgraph load["load"]
            loader["loader"]
        end
    end

    subgraph external["External Dependencies"]
        typedb_lib["typedb.driver"]
        stix2_lib["stix2<br/>library"]
        yaml_lib["yaml"]
        other_libs["stdlib"]
    end

    cli --> commands
    commands --> analysis
    notebook --> analysis
    analysis --> cti_engine
    analysis --> commons

    cti_engine --> result_struct
    cti_engine --> query_stmt
    cti_engine --> commons
    cti_engine --> handler

    handler --> manager
    handler --> builder
    handler --> constants
    handler --> typedb_lib
    handler --> commons

    manager --> typedb_lib
    builder --> dataobjects
    insert --> builder
    insert --> typedb_lib

    orchestrator --> extractor
    orchestrator --> transformer
    orchestrator --> loader
    orchestrator --> commons

    extractor --> extract_const
    extractor --> external

    transformer --> mapper
    transformer --> converter
    mapper --> stix2_lib

    loader --> insert

    commons --> yaml_lib
    commons --> other_libs

    style frontend fill:#f0f0f0,color:#000
    style service fill:#f0f0f0,color:#000
    style engine fill:#f0f0f0,color:#000
    style commons fill:#fff4e6,color:#000
    style datamanagement fill:#f0e6ff,color:#000
    style etl fill:#f0f0f0,color:#000
    style extract fill:#f0e6ff,color:#000
    style transform fill:#f0e6ff,color:#000
    style load fill:#f0e6ff,color:#000
    style external fill:#e6f0e6,color:#000
```


### Commons (Shared functionality)
This package provides common functionality for all the other packages, to ensure consistency and reduce duplication. This includes logging, exception handling, file management and formatting utilities.

### Service (CTI Analysis library)
Provides the `CTIanalysisToolbox` class, the primary public library for CTI analysis operations.

### Engine (Reasoning core)
Provides the `CTIEngine` class which processes predefined semantic queries and runs them over CTI data with automated reasoning enabled. Data classes for results and inferences, and TypeQL query templates are included too.

### Data Management (Persistence layer)
The **typedb** package handles all interactions with a TypeDB database.

### ETL (Data pipeline)
Contains all the classes for executing the Extract → Transform → Load pipeline for CTI data ingestion and insertion into TypeDB.

### Frontend (User interfaces)
Provides a CLI and a sample Jupyter Notebook for running the ETL pipeline and interacting with the analysis library.


### External dependencies

SATRAP depends mainly on TypeDB and STIX2 and YAML libraries.