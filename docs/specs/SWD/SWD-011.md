---
active: true
derived: false
level: 2.1
links:
- ARC-008: tNGY6xykWhhyOON8NhypB6JdUFZeN9xtc1lv9xTwR5A=
normative: true
ref: ''
reviewed: vgZUw3LVkZB238lgdvCtHUXGgl3FvOCjU66PCFNdIqo=
---

# DECIPHER REST service component diagram

This diagram expands the container diagram in ARC-008 at package level. Each node inside the DECIPHER boundary represents either a Python package or a top-level module; arrows show the direction of the call or data dependency.

The two primary entry points exposed by `api` correspond to the analysis flow (triggered by RADAR or an analyst via `POST /analyze/{alert_type}`) and the incident creation flow (triggered via `POST /incident`). In the analysis flow, `analyzers` orchestrates IOC enrichment through `commons.misp_connector`, feeds raw CTI event data to `scoringengine` to compute a severity score, and optionally delegates case creation to `casemanagement`. In the incident flow, `incident_service` resolves the optional template against `casemanagement.template_catalog` before calling `casemanagement.flowintel_connector` directly. The `commons` and `settings` packages provide cross-cutting infrastructure (logging, configuration, external client wrappers) consumed by all other packages.


```mermaid
%% Component diagram — DECIPHER internal architecture (C4 L3 zoom-in)

flowchart TB

    radar(["<b>Wazuh-RADAR</b><br/><i>[External]</i>"])
    misp_ext(["<b>MISP</b><br/><i>[External]</i>"])
    flowintel_ext(["<b>Flowintel</b><br/><i>[External]</i>"])

    subgraph decipher["DECIPHER Service"]
        direction TB

        subgraph api_layer["API Layer"]
            direction LR
            api["<b>api.py</b><br/><i>FastAPI app</i><br/><br/>REST endpoints:<br/>/analyze/{alert_type}<br/>/incident<br/>/analyzers<br/>/health"]
        end

        subgraph middle_row[" "]
            direction LR

            subgraph analysis_layer["Analysis Layer"]
                direction TB

                subgraph registry_group["Analyzer Registry"]
                    registry["<b>AnalyzerRegistry</b><br/><i>analyzers/registry.py</i><br/><br/>Decorator-based registration.<br/>Routes requests to the<br/>correct analyzer instance"]
                end

                subgraph analyzers_group["Analyzers"]
                    base["<b>BaseAnalyzer</b><br/><i>analyzers/base.py</i><br/><br/>Abstract base class.<br/>Defines analyze() contract"]
                    suspicious["<b>SuspiciousLoginAnalyzer</b><br/><i>analyzers/suspicious_login.py</i><br/><br/>Implements suspicious login<br/>threat scenario"]
                    misp_mixin["<b>MISPEnrichmentMixin</b><br/><i>analyzers/mixins/</i><br/><br/>Reusable IOC lookup<br/>& event correlation<br/>via MISPDataExtractor"]
                end

                suspicious -->|"inherits"| base
                suspicious -->|"uses"| misp_mixin
                registry -->|"dispatches to"| suspicious
            end

            subgraph casemanagement_layer["Case Management Layer"]
                direction TB
                incident_svc["<b>incident_service.py</b><br/><br/>create_incident_case().<br/>Template selection +<br/>response assembly"]
                flowintel_conn["<b>flowintel_connector.py</b><br/><br/>create_case_from_bundle().<br/>Template-based case creation<br/>via PyFlowintel"]
                template_catalog["<b>template_catalog.py</b><br/><br/>CASE_TEMPLATES mapping<br/>per AnalysisScenario"]
                incident_svc --> flowintel_conn
                flowintel_conn --> template_catalog
            end
        end

        subgraph scoring_layer["Scoring Engine"]
            direction LR
            misp_score["<b>misp_score.py</b><br/><br/>Noisy-OR aggregation.<br/>score_attribute / score_event<br/>/ score_events"]
            factors["<b>factors.py</b><br/><br/>ThreatLevel, AnalysisStage,<br/>SourceReliability,<br/>InfoCredibility enums<br/>+ ScoringConfig weights"]
            datamodels["<b>datamodels.py</b><br/><br/>EventData, AttributeData,<br/>Sightings, AdmiraltyTags,<br/>FinalScoreResult"]
            misp_score --> factors & datamodels
        end


        subgraph commons_layer["Commons"]
            direction LR
            misp_connector["<b>misp_connector.py</b><br/><br/>MISPDataExtractor.<br/>IOC search, bulk queries,<br/>event correlation via PyMISP"]
            config_loader["<b>config_loader.py</b><br/><br/>mtime-cached YAML loader.<br/>Auto-reloads on file change"]
            log_utils["<b>log_utils.py</b><br/><br/>Structured logging setup"]
        end

        subgraph config_layer["Configuration  (YAML files)"]
            direction LR
            cfg_settings["<b>decipher-settings.yaml</b><br/>MISP URL/key, Flowintel URL,<br/>logging. Needs restart."]
            cfg_runtime["<b>decipher-runtime-cfg.yaml</b><br/>enable_misp_search,<br/>enable_case_creation,<br/>priority thresholds. Hot-reload."]
            cfg_scoring["<b>decipher-scoring-cfg.yaml</b><br/>Scoring weights per factor.<br/>Hot-reload."]
        end
    end

    %% External triggers
    radar           -->|"HTTP POST"| api

    %% API layer routing
    api             -->|"routes analysis"| registry
    api             -->|"routes incident"| incident_svc

    %% Analysis pipeline
	suspicious      -->|"scores events"| misp_score
    suspicious      -->|"enriches IOCs"| misp_mixin
    misp_mixin      -->|"calls"| misp_connector

    %% Commons deps
    misp_connector  -->|"HTTPS REST / PyMISP"| misp_ext
    flowintel_conn  -->|"HTTP REST / PyFlowintel"| flowintel_ext

    %% Config loading (hot-reload via mtime cache)
    config_loader   -.->|"reads"| cfg_runtime & cfg_scoring
    misp_connector  -.->|"reads"| cfg_settings
    flowintel_conn  -.->|"reads"| cfg_settings
    factors         -.->|"reads weights"| cfg_scoring

    %% Styles — API
    style api_layer         fill:#dde8f5,stroke:#1168bd,color:#1168bd
    style api               fill:#1168bd,color:#fff,stroke:#0e4d8c

    %% Styles — Middle row wrapper (invisible)
    style middle_row        fill:transparent,stroke:transparent,color:transparent

    %% Styles — Analysis
    style analysis_layer    fill:#e8f4e8,stroke:#2a7a2a,color:#2a7a2a
    style registry_group    fill:transparent,stroke:#2a7a2a,stroke-dasharray:4 2,color:#2a7a2a
    style analyzers_group   fill:transparent,stroke:#2a7a2a,stroke-dasharray:4 2,color:#2a7a2a
    style registry          fill:#2a7a2a,color:#fff,stroke:#1d5c1d
    style base              fill:#74b374,color:#fff,stroke:#2a7a2a
    style suspicious        fill:#74b374,color:#fff,stroke:#2a7a2a
    style misp_mixin        fill:#a8d4a8,color:#333,stroke:#2a7a2a

    %% Styles — Scoring
    style scoring_layer     fill:#fdf3e3,stroke:#c97b00,color:#c97b00
    style misp_score        fill:#c97b00,color:#fff,stroke:#a36000
    style factors           fill:#e8b84b,color:#333,stroke:#c97b00
    style datamodels        fill:#e8b84b,color:#333,stroke:#c97b00

    %% Styles — Case management
    style casemanagement_layer fill:#f5e8f5,stroke:#7a2a7a,color:#7a2a7a
    style flowintel_conn    fill:#7a2a7a,color:#fff,stroke:#5c1d5c
    style template_catalog  fill:#c4a0c4,color:#333,stroke:#7a2a7a
    style incident_svc      fill:#c4a0c4,color:#333,stroke:#7a2a7a

    %% Styles — Commons
    style commons_layer     fill:#f0f0f0,stroke:#555,color:#333
    style misp_connector    fill:#6b6b6b,color:#fff,stroke:#444
    style config_loader     fill:#9a9a9a,color:#fff,stroke:#777
    style log_utils         fill:#9a9a9a,color:#fff,stroke:#777

    %% Styles — Config
    style config_layer      fill:#fffbe6,stroke:#bba800,color:#7a6e00,stroke-dasharray:4 2
    style cfg_settings      fill:#e8e0a0,color:#333,stroke:#bba800
    style cfg_runtime       fill:#e8e0a0,color:#333,stroke:#bba800
    style cfg_scoring       fill:#e8e0a0,color:#333,stroke:#bba800

    %% Styles — External actors
    style radar             fill:#d45500,color:#fff,stroke:#a33b00
    style misp_ext          fill:#6b6b6b,color:#fff,stroke:#444
    style flowintel_ext     fill:#6b6b6b,color:#fff,stroke:#444
    style decipher          fill:transparent,stroke:#1168bd,stroke-width:2px,color:#1168bd
```
{width="90%"}