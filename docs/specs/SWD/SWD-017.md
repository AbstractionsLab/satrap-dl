---
active: true
derived: false
level: '1.10'
links:
- ARC-014: Urwoa46JUwdp4siI3nHH80Y6eNg21mYfGOgrRjS4AvI=
normative: true
ref: ''
reviewed: GdKOLAcIcK4AB2sq3JFlmJEbCOx3VZhzitAzcQlP6M8=
---

# SATRAP CLI class diagram

This diagram shows a detailed view of the concrete classes, interfaces, and relationships that implement the SATRAP command-line interface and its underlying subsystems.

```mermaid
%% Class diagram showing SATRAP CLI implementation details

classDiagram
    namespace Frontend {
        class satrap_cli {
            -parser: ArgumentParser
            +cli(): void
            -_add_setup(subparsers): void
            -_add_etl(subparsers): void
            -_add_tl(subparsers): void
            -_add_rules(subparsers): void
            -_add_stats(subparsers): void
            -_add_techniques_usage(subparsers): void
            -_add_attck_mitigations(subparsers): void
            -_add_search(subparsers): void
            -_add_info_mitre(subparsers): void
            -_add_mid(subparsers): void
        }

        class commands {
            +as_function(name): function
            +exec_setup(args): void
            +exec_etl(args): void
            +exec_tl(args): void
            +exec_rules(args): void
            +exec_stats(args): void
            +exec_techniques(args): void
            +exec_mitigations(args): void
            +exec_search(args): void
            +exec_info_mitre(args): void
            +exec_mid(args): void
            -_get_etl_kwargs(args): dict
            -_build_exec_end_message(process_name, starttime, endtime, num_data_instances): str
            -_handle_gen_exception(err): void
        }
    }

    namespace ETL {
        class ETLOrchestrator {
            -extractor: Extractor
            -transformer_cls: type[Transformer]
            -loader_cls: type[Loader]
            +extract(source, store_at, **kwargs): void
            +transform(datasrc_path): tuple[list, list, list]
            +load(server_address, db_name, transformed_data): void
            +etl(src, server_address, db_name, **kwargs): void
            +transform_load(data_file, server_address, db_name): void
        }
    }

    namespace Database {
        class typedbmanager {
            +create_database(server_addr, db_name, schema, reset): void$
            +setup_db_schema(schema_session, schema_file): void$
            +fetch_query(server_addr, db_name, query): list$
            +get_query(server_addr, db_name, query, inference): list$
            +aggregate_int_query(server_addr, db_name, query): int$
            +delete_database(server_addr, db_name): void$
            +count_data_instances(server_addr, db_name): int$
        }

        class TypeDBDriver {
            <<external>>
            +core_driver(server_addr): driver
            +session(db_name, session_type): session
            +transaction(transaction_type): transaction
        }
    }

    namespace Analysis {
        class CTIanalysisToolbox {
            -cti_engine: CTIEngine
            +__init__(server, database): void
            +explain_if_related_mitigation(group_mitre_id, mitigation_stix_id): InferredAnswer
            +explain_related_techniques(group_name, technique_id): InferredAnswer
            +explain_techniques_used_by_groups(groups, technique_id): InferredAnswer
            +get_mitre_id(stix_id): str
            +get_sdo_stats(): str
            +get_techniques_used_by_all(groups, infer): dict
            +mitigations_for_technique(stix_id): dict
            +search_by_stix_id(stix_id): dict
            +search_by_mitre_id(mitre_id): list[dict]
            +summarize_techniques_usage(sort_order, used_by_min, used_by_max, infer, revoked, limit): list
            +mitre_attack_mitigations(): str
        }

        class CTIEngine {
            -server: str
            -database: str
            -session: TypeDBSession
            +__enter__(): CTIEngine
            +__exit__(): void
            +search_obj_by_attck_id(attck_id): list[dict]
            +explain_group_rel_mitig(group_stix_id, mitigation_stix_id): InferredAnswer
            +explain_mitig_rel_tech(group_name, technique_stix_id): InferredAnswer
            +explain_techniques_used_by(groups, technique_stix_id): InferredAnswer
            +get_mitre_id(stix_id): str
            +get_stats(): list[dict]
            +get_inference_rule_names(): list[str]
        }
    }

    %% Relationships

    %% Frontend to ETL
    commands --> ETLOrchestrator: instantiates & invokes
    commands --> typedbmanager: calls database operations

    %% Frontend to Analysis
    commands --> CTIanalysisToolbox: instantiates & invokes
    satrap_cli --> commands: routes commands to

    %% ETL Orchestrator relationships
    ETLOrchestrator --> typedbmanager: loads data via

    %% TypeDB interactions
    typedbmanager --> TypeDBDriver: uses

    %% Analysis relationships
    CTIanalysisToolbox --> CTIEngine: composes
    CTIEngine --> TypeDBDriver: queries database via

    %% Note
    note for ETLOrchestrator "Orchestrates three-phase process: extract → transform → load"
    note for CTIEngine "Context manager: enables transaction management"
```
{width="80%"}

The CLI commands rely on the `ETLOrchestrator` and the `CTIanalysisToolbox` to coordinates the three-phase extract- transform - load pipeline and to perform specific analysis methods (e.g., `explain_if_related_mitigation`) while hiding query complexity.