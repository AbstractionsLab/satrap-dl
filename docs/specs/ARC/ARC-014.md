---
active: true
derived: false
level: 1.7
links:
- SRS-038: ncqnw4s_4Ge1gkf0tjXyL2u6i_Vyt-smjqcK3jJYiqI=
- SRS-042: K9A_2Phzz_IzEOuXzTthssUw6LnZFxT-lJee-UjA4RI=
normative: true
ref: ''
reviewed: Js5jjQj-Ra3X0MX3uKc8JoB6AAJvDGnB0IxcZEAs95k=
---

# SATRAP command-line interface design

The SATRAP command-line interface (CLI) is primarily intended for setting up a fresh CTI knowledge base and executing an extract-transform-load (ETL) pipeline to populate the knowledge base with STIX 2.1 data.

A minimal set of analytical functions will be made available through this interface, however, the main interface for CTI analysis is foreseen to be Jupyter notebooks using the `CTIAnalysis Toolbox`, which provides a larger set of functions.

The following diagram depicts the main CLI components and their interactions with the underlying subsystems.

```mermaid
%% C4 Component diagram — SATRAP CLI architecture

graph TB
    user["<b>User</b><br/><i>[External Actor]</i><br/><br/>Operator executing SATRAP<br/>commands via terminal"]

    subgraph boundary["SATRAP CLI"]
        direction LR

        parser["<b>CLI Parser</b><br/><i>[Component: satrap_cli.py]</i><br/><br/>Argument parser and command router.<br/>Handles command-line arguments,<br/>builds subcommand menus,<br/>and dispatches to execution functions."]

        commands["<b>Command Handlers</b><br/><i>[Component: commands.py]</i><br/><br/>Implementation of execution logic for each command:<br/>setup, etl, tl, rules, stats,<br/>techniques, mitigations, etc."]
    end

    subgraph etl_subsys["ETL Subsystem"]
        orchestrator["<b>ETL Orchestrator</b><br/><i>[Component: ETLOrchestrator]</i><br/><br/>Coordinates extraction, transformation,<br/>and loading of STIX 2.1 data.<br/>Supports MISP and file sources."]
    end

    subgraph db_subsys["Database Subsystem"]
        db_mgr["<b>TypeDB Manager</b><br/><i>[Component: typedbmanager]</i><br/><br/>Creates and manages TypeDB databases.<br/>Initializes schema and inference rules.<br/>Manages database lifecycle."]
    end

    subgraph cti_subsys["CTI Analysis Subsystem"]
        analysis["<b>CTI Analysis Service</b><br/><i>[Component: CTIanalysisToolbox]</i><br/><br/>High-level CTI reasoning engine.<br/>Executes analysis queries and<br/>returns formatted results."]
    end

    subgraph external["External Systems"]
        typedb["<b>TypeDB Server</b><br/><i>[External System]</i><br/><br/>Knowledge base storing<br/>CTI and inference rules"]

        sources["<b>Data Sources</b><br/><i>[External System]</i><br/><br/>MISP instances, MITRE ATT&CK, STIX(2.1)-compliant feeds"]
    end

    user -->|execute commands| parser
    parser -->|route command| commands
    commands -->|setup command| db_mgr
    commands -->|etl/tl commands| orchestrator
    commands -->|analysis commands| analysis

    orchestrator -->|load STIX 2.1 data| db_mgr
    db_mgr -->|create/manage| typedb
    orchestrator -->|extract from| sources
    analysis -->|query| typedb

    style parser fill:#4A90E2,stroke:#2E5C8A,color:#fff
    style commands fill:#4A90E2,stroke:#2E5C8A,color:#fff
    style orchestrator fill:#F5A623,stroke:#C67E1A,color:#fff
    style db_mgr fill:#F5A623,stroke:#C67E1A,color:#fff
    style analysis fill:#7ED321,stroke:#5A9E1A,color:#fff
    style typedb fill:#E8E8E8,stroke:#999,color:#000
    style sources fill:#E8E8E8,stroke:#999,color:#000
    style user fill:#E8E8E8,stroke:#999,color:#000
```
{width="75%"}

The SATRAP CLI provides the following command groups:

### Database and ETL commands

- **`setup`**: Create a fresh CTI semantic knowledge base with schema and inference rules. Options: `--database`, `--delete`, `--testmode`
- **`etl`**: Full Extract-Transform-Load pipeline from MISP or file sources. Options: `--xmode`, `--src`, `--database`, `--apikey`, `--test`
- **`tl`**: Transform-Load only, for working with pre-extracted STIX files. Options: `--src`, `--database`

### Analysis commands

- **`rules`**: Display all inference rules defined in the knowledge base
- **`stats`**: Show statistics of STIX Domain Objects (SDOs) by type
- **`techniques`**: List all MITRE ATT&CK techniques in the knowledge base with optional filtering
- **`mitigations`**: Display all MITRE ATT&CK mitigations
- **`search`**: Search for STIX objects by STIX ID, MITRE ID, or name/alias
- **`info-mitre`**: Retrieve detailed information about a threat group
- **`mid`**: Convert a STIX ID to its MITRE ATT&CK ID

### Default flags

- **`--help`**: Show help message with command usage and options
- **`--version`**: Display the current version of SATRAP

## Component interactions
The role of each depicted component is summarized next:

### CLI Parser (`satrap_cli.py`)

- Initializes argument parser with global options (server URI, timeout)
- Builds subcommand menus for all commands
- Routes user input to appropriate command handler
- Handles keyboard interrupts and exceptions

### Command Handlers (`commands.py`)

Each command handler (`exec_*` function) implements:
- Argument validation and default configuration
- Error handling specific to the command
- Integration with backend subsystems
- Result formatting and display to user

### Subsystem Integration

- **ETL operations**: Commands invoke `ETLOrchestrator` for data ingestion and `TypeDB Manager` for schema initialization
- **Database operations**: Setup command uses `TypeDB Manager` to create fresh instances
- **Analysis operations**: Analysis commands delegate to `CTIanalysisToolbox` for intelligent querying