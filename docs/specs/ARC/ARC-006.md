---
active: true
derived: false
level: 2.2
links:
- SRS-048: uW7UdT-0pxk5yTKagK_woeUO-K0EBzL8UGw6fsVW6hc=
- SRS-058: D58uPbMzuO-g5KdY9Wk78nxADN6jsITxgc2A_M4rxpU=
normative: true
ref: ''
reviewed: il5jooyGtksprNDKppORBXhLrilIGuVglypXtkCkK9E=
---

# DECIPHER infrastructure deployment diagram

The DECIPHER infrastructure stack consists of four containerized services: the **DECIPHER REST analysis API**, the **MISP** threat intelligence platform, the **Flowintel** case management system, and the **Wazuh** SIEM with the **RADAR** active response module acting as the external alert source. The deployment of Wazuh/RADAR falls outside the scope of SATRAP-DL, yet, we include it here for depicting the complete context of the DECIPHER infrastructure.

The diagram depicts a representative two-host deployment, yet, the number of nodes is flexible thanks to the containerized architecture. The Wazuh/RADAR subsystem runs on a dedicated IDPS-ESCAPE host and triggers analysis by posting alerts to the DECIPHER API over the network. MISP, Flowintel, and the DECIPHER API container are co-located on the SATRAP-DL host, each isolated in its own Docker network. A `config` volume mount supplies runtime configuration to the API container without rebuilding the image.


```mermaid
%% Deployment diagram — DECIPHER infrastructure stack

flowchart TB

    subgraph wazuh_host["IDPS-ESCAPE Docker host"]
        direction TB
        subgraph wazuh_stack["Wazuh Manager"]
            direction LR
            wazuh_mgr["<b>wazuh-manager</b><br/><i>wazuh/wazuh-manager</i><br/><br/>SIEM & alert engine"]
            radar["<b>RADAR</b><br/><i>[Subsystem]</i><br/><br/>Active Response module.<br/>Triggers DECIPHER on alert"]
            wazuh_mgr --> radar
        end
    end

    subgraph host["SATRAP-DL Docker host"]
        direction TB

        subgraph decipher_stack["DECIPHER |  network: satrap-dl-net"]
            direction LR
            api["<b>api</b><br/><i>decipher/api:latest</i><br/><br/>Uvicorn + FastAPI<br/>port 8000"]
            cfg[("config/<br/><i>volume mount</i>")]
            cfg --- api
        end

        subgraph misp_stack["MISP  |  network: misp-net"]
            direction LR
            misp_core["<b>misp-core</b><br/><i>misp-docker/misp-core</i><br/><br/>MISP web app<br/>port 443"]
            misp_mod["<b>misp-modules</b><br/><i>misp-docker/misp-modules</i><br/><br/>Enrichment modules"]
            misp_guard["<b>misp-guard</b><br/><i>misp-docker/misp-guard</i><br/><br/>Request filtering"]
            misp_db[("db<br/><i>MariaDB 10.11</i>")]
            misp_redis[("redis<br/><i>Valkey 7.2</i>")]
            misp_mail["mail<br/><i>SMTP relay</i>"]
            misp_core --> misp_db & misp_redis & misp_mod
            misp_guard --> misp_core
            misp_core --> misp_mail
        end

        subgraph flowintel_stack["Flowintel |  network: flowintel-net"]
            direction LR
            flowintel_app["<b>flowintel</b><br/><i>flowintel/flowintel:2.3.0</i><br/><br/>Case management app<br/>port 7006"]
            flowintel_db[("postgresql<br/><i>PostgreSQL 17</i>")]
            flowintel_cache[("valkey<br/><i>Valkey</i>")]
            flowintel_app --> flowintel_db & flowintel_cache
        end
    end

    radar   -->|"HTTP POST :8000<br/>/api/v0.1/analyze/{alert_type}"| api
    api -->|"PyMISP / HTTPS :443"| misp_core
    api -->|"PyFlowintel / HTTP :7006"| flowintel_app

    style wazuh_host     fill:transparent,stroke:#d45500,stroke-width:2px,color:#d45500,stroke-dasharray:6 3
    style wazuh_stack    fill:#fde8d8,stroke:#d45500,stroke-width:1.5px,color:#d45500
    style wazuh_mgr      fill:#d45500,color:#fff,stroke:#a33b00
    style radar          fill:#f5a06e,color:#fff,stroke:#d45500
    style host            fill:transparent,stroke:#555,stroke-width:2px,color:#333,stroke-dasharray:6 3
    style decipher_stack  fill:#dde8f5,stroke:#1168bd,stroke-width:1.5px,color:#1168bd
    style misp_stack      fill:#e8e8e8,stroke:#555,stroke-width:1.5px,color:#333
    style flowintel_stack fill:#e8e8e8,stroke:#555,stroke-width:1.5px,color:#333
    style api             fill:#1168bd,color:#fff,stroke:#0e4d8c
    style cfg             fill:#cfe2f3,color:#333,stroke:#1168bd
    style misp_core       fill:#6b6b6b,color:#fff,stroke:#444
    style misp_mod        fill:#9a9a9a,color:#fff,stroke:#777
    style misp_guard      fill:#9a9a9a,color:#fff,stroke:#777
    style misp_db         fill:#c8c8c8,color:#333,stroke:#777
    style misp_redis      fill:#c8c8c8,color:#333,stroke:#777
    style misp_mail       fill:#c8c8c8,color:#333,stroke:#777
    style flowintel_app   fill:#6b6b6b,color:#fff,stroke:#444
    style flowintel_db    fill:#c8c8c8,color:#333,stroke:#777
    style flowintel_cache fill:#c8c8c8,color:#333,stroke:#777
```
{width="75%"}


### Channels between trust boundaries

SRS-058 requires TLS 1.2 or higher for all data in transit crossing a trust boundary.
In the depicted diagram this would apply to the connections between the Wazuh/RADAR subsystem and the DECIPHER API if the IDPS-ESCAPE and SATRAP-DL hosts are located in different trust zones.

Possible solutions include:
- Supporting HTTPS in the REST API and configuring the Wazuh/RADAR subsystem to use HTTPS when posting alerts to the DECIPHER API. This would require setting up TLS certificates on the DECIPHER host and configuring the API to serve over HTTPS.
- Deploying the IDPS-ESCAPE and SATRAP-DL hosts inside the same trust boundary, defined as a network environment where inter-host communication is restricted to authorized endpoints and protected from external access by a common perimeter control (e.g. a dedicated VPN or an isolated network segment with enforced in/out filtering).