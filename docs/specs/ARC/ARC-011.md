---
active: true
derived: false
level: 2.8
links:
- SRS-055: 8gjVB5NQ7tpA-nFGs0PdA4hP22FWH0f7gwfq1lPZ514=
normative: true
ref: ''
reviewed: aVLf6zieUPr3BcmDkGZc7mRLzd12J5HI3PrQS81YqX0=
---

# REST service: incident endpoint interaction

## Initial approach: template catalog bundled in the image

The incident creation endpoint receives a bundle with a priority level from the MISP `priority-level` taxonomy and creates a case in Flowintel assigning it the corresponding priority tag. An optional `template_id` in the request body selects a scenario-specific case template from a template catalog initially pre-built in the case management module; when no template with such an id is found the case is created without one. Any additional key-value pairs provided in the request body are serialized into the case description in a readable format.

The diagram below shows the system-level interaction flow for a POST request to the incident endpoint.

```mermaid
%% Option A — both template content and bindings are bundled inside the image

flowchart LR
    analyst(["<b>User</b><br/><i>[Human or system]</i>"])
    flowintel["<b>Flowintel</b><br/><i>[External System]</i><br/>PyFlowintel / HTTP :7006"]

    subgraph decipher["DECIPHER  [Container: FastAPI/Uvicorn]"]
        direction TB
        api["<b>REST API layer</b><br/>POST /incident"]
        incident["<b>Incident service</b>"]

        subgraph casemanagement["Case management"]
            direction LR
            catalog["<b>TemplateCatalog</b><br/><br/>JSON files bundled in image<br/>mapping wired in code<br/><i>(fully developer-managed)</i>"]
            connector["<b>Flowintel connector</b>"]
        end
    end

    analyst  -->|"HTTP POST /incident<br/>{priority, template_id, title, description_bundle}"| api
    api      --> incident
    incident --> catalog
    catalog  --> connector
    connector -->|"PyFlowintel - create case"| flowintel

    style analyst     fill:#08427b,color:#fff,stroke:#083566
    style flowintel   fill:#6b6b6b,color:#fff,stroke:#444
    style decipher    fill:transparent,stroke:#1168bd,stroke-width:2px,color:#1168bd
    style casemanagement fill:transparent,stroke:#4a90d9,stroke-width:1px,stroke-dasharray:4 2,color:#1168bd
    style api         fill:#1168bd,color:#fff,stroke:#0e4d8c
    style incident    fill:#4a90d9,color:#fff,stroke:#1168bd
    style catalog     fill:#4a90d9,color:#fff,stroke:#1168bd
    style connector   fill:#4a90d9,color:#fff,stroke:#1168bd
```

## Future work: Flowintel central template repository when supported

When a centralized templates repository is fully supported in Flowintel (in development as of March 2026), the creation and management of templates will be offloaded to Flowintel as this is already part of the tool's responsibilities and aligns with the principle of separation of concerns.

DECIPHER queries Flowintel at case-creation time to resolve the appropriate template by a known identifier (e.g. a provided template ID). The API contract of the incident endpoint would remain unchanged, but the implementation of the incident service would need to accommodate the new template discovery mechanism and the dynamic nature of the available templates.

```mermaid
%% Flowintel is the authoritative template store; DECIPHER queries it at runtime

flowchart LR
    analyst(["<b>User</b><br/><i>[System or human]</i>"]):::user
    incident_resp(["<b>Incident analyst</b><br/><i>[Person]</i>"]):::user

    subgraph decipher["DECIPHER  [Container: FastAPI/Uvicorn]"]
        direction TB
        api["<b>REST API layer</b><br/>POST /incident"]
        incident["<b>Incident service</b>"]
        connector["<b>Flowintel connector</b><br/><i>resolves template ID at runtime</i>"]
        incident --> connector
    end

    subgraph flowintel_boundary["Flowintel  [External System]"]
        direction LR
        fl_gui["<b>Flowintel UI</b>"]:::fl_ui
        fl_api["<b>Flowintel API</b>"]:::fl_ui
        fl_templates[("<b>case_templates</b><br/><i>internal store</i><br/>operator-managed via<br/>Flowintel UI or API")]
        fl_cases[("<b>cases</b><br/><i>internal store</i>")]
        fl_api --> fl_templates
        fl_api --> fl_cases
    end

    analyst  -->|"HTTP POST /incident<br/>{priority, template_id, title, description_bundle}"| api
    incident_resp  -->|"manage templates"| fl_api
    incident_resp  -->|"manage templates"| fl_gui
    api      --> incident
    connector -->|"PyFlowintel - create case from template"| fl_api
    connector -->|"PyFlowintel - create case (no template)"| fl_api

    style decipher           fill:transparent,stroke:#1168bd,stroke-width:2px,color:#1168bd
    style flowintel_boundary fill:transparent,stroke:#6b6b6b,stroke-width:2px,color:#555
    style api                fill:#1168bd,color:#fff,stroke:#0e4d8c
    style incident           fill:#4a90d9,color:#fff,stroke:#1168bd
    style connector          fill:#4a90d9,color:#fff,stroke:#1168bd
    style fl_templates       fill:#999,color:#fff,stroke:#6b6b6b
    style fl_cases           fill:#999,color:#fff,stroke:#6b6b6b

    classDef user fill:#08427b,color:#fff,stroke:#083566
    classDef fl_ui fill:#6b6b6b,color:#fff,stroke:#444
```