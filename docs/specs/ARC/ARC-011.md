---
active: true
derived: false
level: 2.8
links:
- SRS-055: 8gjVB5NQ7tpA-nFGs0PdA4hP22FWH0f7gwfq1lPZ514=
normative: true
ref: ''
reviewed: 6C6O9SRKH68kPocD3MpFdAS3W7Z0rabqpjWqTbIp1Uw=
---

# REST service: incident endpoint interaction

The incident creation endpoint receives a bundle with a priority level from the MISP `priority-level` taxonomy and creates a case in Flowintel assigning it the corresponding priority tag.
If an optional `template_id` is provided in the request body, the case will be created based on the corresponding template; when no template with such an id is found the case is created without one.

In alignment with the separation of concerns principle, the creation and management of templates will be offloaded to Flowintel as this feature is already provided by the tool and further enhanced through a central repository of templates.

DECIPHER queries Flowintel at case-creation time to resolve the appropriate template from the given identifier, which is expected to be a valid template ID in the central templates repository of Flowintel.

Any additional key-value pairs provided in the request body are serialized into the case description in a readable format.

The diagram below shows the system-level interaction flow for a POST request to the incident endpoint.


```mermaid
%% Flowintel is the authoritative template store; DECIPHER queries it at runtime

flowchart LR
    analyst(["<b>User</b><br/><i>[System or human]</i>"]):::user
    incident_resp(["<b>Incident analyst</b><br/><i>[Person]</i>"]):::user

    subgraph decipher["DECIPHER  [Container: FastAPI/Uvicorn]"]
        direction TB
        api["<b>REST API layer</b><br/>POST /incident"]
        incident["<b>Incident service</b>"]
        connector["<b>Flowintel connector</b><br/><i>resolves template ID at runtime</i>"]
        incident --> connector
    end

    subgraph flowintel_boundary["Flowintel  [External System]"]
        direction LR
        fl_gui["<b>Flowintel UI</b>"]:::fl_ui
        fl_api["<b>Flowintel API</b>"]:::fl_ui
        fl_templates[("<b>central templates<br/> repository</b><br/><i>internal store</i><br/>operator-managed via<br/>Flowintel UI or API")]
        fl_cases[("<b>cases</b><br/><i>internal store</i>")]
        fl_gui --> fl_templates
        fl_api --> fl_templates
        fl_api --> fl_cases
    end

    analyst  -->|"HTTP POST /incident<br/>{priority, template_id, title, description_bundle}"| api
    incident_resp  -->|"manage templates"| fl_api
    incident_resp  -->|"manage templates"| fl_gui
    api      --> incident
    connector -->|"PyFlowintel - create case from template"| fl_api
    connector -->|"PyFlowintel - search template by id"| fl_api
    connector -->|"PyFlowintel - create case (no template)"| fl_api

    style decipher           fill:transparent,stroke:#1168bd,stroke-width:2px,color:#1168bd
    style flowintel_boundary fill:transparent,stroke:#6b6b6b,stroke-width:2px,color:#555
    style api                fill:#1168bd,color:#fff,stroke:#0e4d8c
    style incident           fill:#4a90d9,color:#fff,stroke:#1168bd
    style connector          fill:#4a90d9,color:#fff,stroke:#1168bd
    style fl_templates       fill:#999,color:#fff,stroke:#6b6b6b
    style fl_cases           fill:#999,color:#fff,stroke:#6b6b6b

    classDef user fill:#08427b,color:#fff,stroke:#083566
    classDef fl_ui fill:#6b6b6b,color:#fff,stroke:#444
```