[tool.poetry]
name = "satrap-dl"
version = "1.0"
description = "SATRAP-DL (Semi-Automated Threat Reconnaissance and Analysis Powered by DECIPHER Logic) is a suite for logic-based CTI analysis (satrap) and automated incident handling (decipher)."
authors = ["Itzel Vazquez Sandoval <itzel@abstractionslab.lu>", "Arash Atashpendar <arash@abstractionslab.lu>", "Sven Angel <sven.angel@proton.me>"]
readme = "README.md"
packages = [
    { include = "satrap" },
    { include = "decipher" },
]

[tool.poetry.dependencies]
python = ">=3.11.0,<3.13"
pyyaml = "^6.0.2"
pymisp = "^2.5.32.2"

# SATRAP dependencies
[tool.poetry.group.satrap.dependencies]
typedb-driver = "^2.29.0"
requests = "^2.32.3"
jsonschema = "^4.23.0"
stix2 = "^3.0.1"
tabulate = "^0.9.0"

# DECIPHER dependencies
[tool.poetry.group.decipher.dependencies]
fastapi = "^0.141.1"
uvicorn = { version = "^0.27.0", extras = ["standard"] }
pydantic = "^2.12.5"
httpx2 = "^2.9.1"
pyflowintel = { git = "https://github.com/AbstractionsLab/PyFlowintel.git", branch = "main" }

# Doorstop-based specification tooling (docs/specs); not needed to run satrap or decipher
[tool.poetry.group.specs]
optional = true

[tool.poetry.group.specs.dependencies]
doorstop = "3.0b10"
# doorstop 3.0b10 imports 'pkg_resources', removed in setuptools 82.0.0
setuptools = "<81.0.0"

[tool.poetry.group.notebooks]
optional = true

[tool.poetry.group.notebooks.dependencies]
ipykernel = "^6.29.5"
ipywidgets = "^8.1.5"

# Development tooling; not needed to run satrap or decipher
[tool.poetry.group.dev]
optional = true

[tool.poetry.group.dev.dependencies]
black = "^26.5.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
satrap = "satrap.frontend.satrap_cli:cli"
